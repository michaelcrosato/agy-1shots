# Notion Scraper

A self-contained web scraper that extracts page content and updates a Notion database.

## Setup

Install dependencies:
`npm install`

## Configuration

- `NOTION_TOKEN`: Notion integration token.
- `NOTION_DATABASE_ID`: Target database ID.
- `MOCK`: Set to `true` to run a local mock test.

## Usage

`npm start`
